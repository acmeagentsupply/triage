You are building the public open-source repo for OCTriageUnit — a read-only OpenClaw control plane triage tool. Working directory is /Users/AGENT/octriageunit/

TRUST DOCTRINE (non-negotiable — every file must honour these):
1. READ-ONLY BY DEFAULT: no config mutation, no service restarts, no network writes, proof bundle written only under user home
2. NO SECURITY THROUGH OBSCURITY: no hidden calls, no telemetry, no phone-home
3. SOURCE-VISIBLE FIRST: reproducible build, checksum verification, clear dependency list
4. LATENT FEATURE DISCIPLINE: future hooks inert by default, user-activated only
5. PROOF-BUNDLE CULTURE: every diagnostic claim backed by artifacts (bundle_summary.txt, gateway_err_tail.txt, launchctl snapshots, doctor output)
6. OPERATOR-FIRST UX: fast, deterministic, safe under degraded systems, non-blocking

DELIVERABLES — create ALL of these:

1. README.md (replace stub)
   - What OCTriageUnit is (1 paragraph)
   - SAFETY GUARANTEES section (bold, prominent):
     * Read-only: never modifies system state
     * No telemetry: zero outbound calls
     * Local-only: all execution on operator machine
     * Proof bundle: written to ~/octriage-bundles/ only
     * Auditable: how to inspect the script (cat bin/control-plane-triage)
   - USAGE section: basic CLI invocation
   - HOW TO VERIFY section: SHA256 check, build from source
   - THREAT MODEL (brief): what it does, what it cannot do, trust boundary
   - LICENSE note (MIT)

2. LICENSE
   MIT License. Copyright 2026 ACME Agent Supply Co.

3. SECURITY.md
   - Threat model: what OCTriageUnit reads, what it never touches
   - Responsible disclosure: security@acmeagent.co (placeholder)
   - Supported versions table
   - Build verification steps: how to reproduce SHA256 from source
   - What to do if you find a hidden outbound call (it's a bug, report it)

4. docs/trust-doctrine.md
   Copy and adapt the full trust doctrine (6 principles + release checklist + strategic funnel note). Customer-facing language, no internal jargon.

5. bin/control-plane-triage
   A bash script stub that:
   - Has a prominent header comment block stating: READ-ONLY MODE, NO NETWORK CALLS, NO CONFIG MUTATION
   - Accepts --help flag that prints safety guarantees before usage
   - Has a check_env() function that verifies required tools present (launchctl, openclaw) without calling them
   - Has a proof_bundle() function stub that writes output to ~/octriage-bundles/$(date +%Y%m%d-%H%M%S)/
   - All actual triage logic stubbed as TODO comments
   - Must be chmod +x
   - Every function has a "SAFE:" comment confirming what it does NOT do

6. docs/proof-bundle-format.md
   Documents the standard proof bundle output format:
   - bundle_summary.txt (what it contains)
   - gateway_err_tail.txt (last 200 lines, filtered)
   - launchctl_snapshot.txt (launchctl list output)
   - doctor_output.txt (openclaw doctor output)
   - manifest.sha256 (SHA256 of all files in bundle)

CONSTRAINTS:
- All files must be complete, not stubs with "TODO fill in" — except where explicitly noted above
- bin/control-plane-triage must pass `bash -n` (syntax check)
- README must be genuinely useful, not marketing fluff
- No placeholder email addresses except the security one (security@acmeagent.co)
- SECURITY.md threat model must be technically accurate to what a bash triage script can/cannot do

When completely finished, run:
openclaw system event --text "Done: OCTriageUnit trust hardening pass complete — README/LICENSE/SECURITY.md/docs/bin all created" --mode now
